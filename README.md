# OpenClaw One-Page Site

A self-contained, responsive landing page that explains what OpenClaw is, how it works, and why an open agent workflow layer is useful.

## Run locally

Open `index.html` directly in a browser, or serve the directory with any static file server.

## Test

```sh
node --test
```
